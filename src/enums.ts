
     export enum Permissions{
        User =1,
        Driver =2,
        Admin =3,
      } 
      export enum Statuses{
        No_order = 0,
        Have_order = 1,
        Order_close = 2
      }
      export enum TypePath{
        Direct = 'ישיר',
        Reguarded = 'מאסף'
      }
       export enum Order_states{
         Acccept = 1,
         DisAccept = 0
       }

